# Ids de bloco em 16 bits — Plano de implementação

> Anotado em 2026-09-12 (sessão 98), a pedido do usuário ("anota um plano pra aumentar o número
> de ids"). **IMPLEMENTADO no mesmo dia** ("pode fazer os ids em 16 bits e um plano de auto
> converter os mundos antigos automaticamente") — Opção A + conversão automática (seção
> própria abaixo). Checkboxes marcados com o que foi feito e como foi provado.

## Por que agora

Cada célula de chunk é **1 byte** (`Uint8Array`, `shared/src/world.ts:29`), então o teto é 256
ids. Depois da cabeceira da cama (bug-662, ids 247-250) sobram **5 livres (251-255)**. Os
circuitos lógicos do 9º ano (o coração do "Lógica em Jogo") vão precisar de dezenas de blocos
(fio, porta E/OU/NÃO, lâmpada, alavanca, cada um com estado/direção). Sem aumentar, o próximo
bloco com 4 direções já não cabe. E a porta ficou sem conserto por id (seriam 8) — só saiu
porque o par por posição dava conta (sessão 98).

## Decisão recomendada: Opção A — chunk `Uint16Array`, gravado/enviado ESTREITO quando dá

- **Na memória:** todo chunk vira `Uint16Array` (65.536 valores). Memória do bloco dobra: mundo
  máximo 256×256×128 = 8,4 M células → 16,8 MB (hoje 8,4). No lazy (E) só as colunas residentes.
- **No disco e no fio:** cada chunk leva 1 byte de LARGURA — `0` = estreito (u8, todos os ids ≤
  255), `1` = largo (u16). **Todo mundo que existe hoje continua do mesmo tamanho** no save e no
  Wi-Fi da escola (os `.ljw` de aula têm ~1,4 MB; tablet puxa colunas por streaming). Só chunk
  que tiver bloco ≥ 256 paga o dobro.
- **Teto novo: bloco < 900.** Os ITENS começam em 900 (`ITEM_BALDE_VAZIO`, `blocks.ts:367`) e
  moram no mesmo espaço numérico da mochila. Bloco vai de 0 a 899 (3,5× o de hoje); se um dia
  faltar, move-se a faixa de item (os ids de item são byte de SAVE também — mochila/baú).
- **Formatos antigos são lidos PRA SEMPRE** (`LJW0`, `LJS1`, `LJS2`): u8 → u16 na leitura. Os
  `.ljw` de `cenarios/` e os saves de IndexedDB do singleplayer não precisam ser regerados.

### Rejeitada: Opção B — 2º byte de "estado" por célula

Separar direção/estágio/aberta/nível do id liberaria ~100 ids (famílias de 4 direções, 8
fornalhas, 7 águas, estágios de planta, portas/janelas). Mas toca os MESMOS arquivos da A e mais
TODA regra e todo ramo do mesher que hoje lê o estado no id (`camaHeadDir`, `portaToggled`,
`aguaNivel`, `escadaId`, `fornalhaComFrente`…), e rende 256 + ~100 contra os 900 da A. Só vale
se memória/banda virar problema medido — e a largura por chunk da A já cobre a banda.

## Mapa do que assume "1 byte por bloco" (levantado em 2026-09-12)

| Onde | O que | Muda para |
|---|---|---|
| `shared/src/world.ts:29,37,57` | `chunks: (Uint8Array \| undefined)[]`, alocação | `Uint16Array` |
| `shared/src/protocol.ts:1162-1225` | snapshot `LJW0` (dims u8 + chunks crus) | `LJW1` com byte de largura por chunk; decode lê os dois |
| `shared/src/protocol.ts:1300-1360` | lote de colunas `encodeColunas`/decode (streaming) | magia nova + largura por chunk |
| `shared/src/save.ts:45,158-253` | `LJS2` lazy: `CHUNK_VOLUME` bytes por chunk | `LJS3` com largura; lê `LJS2` |
| `shared/src/save.ts:144` | `LJS1` = header + snapshot | passa a embrulhar `LJW1` (magia `LJS1` pode ficar — o snapshot se identifica) |
| `shared/src/mesher.ts:756-840` | vizinhança `viz = new Uint8Array(VIZ_VOLUME)` | `Uint16Array` |
| `shared/src/worldgen.ts:218` | `saida: Uint8Array` | **fica u8** — é a máscara de caverna, não id (engano do levantamento) |
| `shared/src/luz.ts:125` | grade de LUZ (0-15 céu + 0-15 bloco) | **fica u8** — é luz, não id |
| `shared/src/rules.ts:455` | `for (let id = 0; id < 256; id++)` monta o mapa de regras | `id <= MAX_BLOCK_ID` |
| `shared/src/session.ts:597-602`, `save.ts:141` | `editedChunks: { bytes: Uint8Array }` | `Uint16Array` |
| `client/src/chunks.ts:452`, `meshPool.ts:161`, `meshWorker.ts:16` | `viz` transferida pro worker | `Uint16Array` (transferable, mesmo caminho) |
| `server/src/cenarios/gerar.ts:456,473` | grava `.ljw` | nada (usa `encodeSave`), só regerar se quiser |
| `shared/src/blocks.ts:324` | `MAX_BLOCK_ID` | ganha portão: `< ITEM_BALDE_VAZIO` |

Mensagens JSON com `blockId` (`place_block`, `block_update`, `/regiao`) já são `number` — nada.

## Conversão automática dos mundos antigos (pedido do usuário)

Não há passo manual nem comando. **Ler** os formatos antigos é permanente (`decodeSave` aceita
LJS1+LJW0 e LJS2 e marca `legado: true`); **converter** é só gravar de novo, e o `encodeSave`
já escreve o formato novo. O que foi acrescentado é a rede de segurança antes da 1ª gravação
por cima:

| Onde o mundo mora | O que acontece ao abrir um save antigo |
|---|---|
| Host, cópia viva (`mundos/<nome>/<nome>.ljw`) — boot (`server/src/index.ts`) | copia o original pra `<nome>.antes-ids16.ljw` na mesma pasta (só se ainda não existe) e **grava já** no formato novo, sem esperar o autosave |
| Host, troca ao vivo (`/mundo carregar`, `server/src/mundos.ts`) | guarda o original do mesmo jeito; o autosave da sessão nova converte |
| Singleplayer (IndexedDB, `client/src/main.ts` `persistWorld`) | na 1ª gravação, o registro ganha `dataAntesIds16` com os bytes originais (`saveEhLegado` olha só o cabeçalho) |
| Modelo em `cenarios/` e mundo de aula (só leitura) | nada é escrito — continuam no formato antigo e são lidos igual |

A cópia `.antes-ids16.ljw` não aparece como mundo novo: o host e os launchers só listam
`mundos/<nome>/<nome>.ljw`. Provado no host real (save LJW0 real → cópia idêntica byte a byte,
arquivo novo LJW1, 2ª subida sem conversão) e no navegador real (registro antigo no IndexedDB →
jogar → sair → `data` em LJW1 + `dataAntesIds16` idêntico ao original).

**Ainda não feito (se precisar):** restaurar o backup pelo menu do singleplayer, e regravar os
`.ljw` de `cenarios/` no formato novo (`npm run cenarios`) — não é necessário pra funcionar.

## Tarefas

### Tarefa 1 — Portões antes de mexer (testes que têm de continuar verdes)
- [x] Teste: todo `BlockId` < `ITEM_BALDE_VAZIO` (e `MAX_BLOCK_ID` também) — `ids16.test.ts`.
- [x] Hash FNV dos blocos de `cenarios/aula1-sequencia.ljw` e `server/world.ljw` medido com o
      código de 1 byte ANTES da troca (20783349 / 4158559134) — idêntico depois.
- [x] LJW0, LJC0 e LJS2 montados à mão no teste e lidos pelo código novo.

### Tarefa 2 — Memória: `World` em `Uint16Array`
- [x] `world.ts`: tipo `ChunkBlocos = Uint16Array`. `getBlock`/`setBlock` iguais.
- [x] `worldgen.ts` NÃO muda: o `saida: Uint8Array` é a máscara de caverna, não id de bloco.
- [x] `mesher.ts` `viz` → `Uint16Array` + cliente (`chunks.ts`, `meshPool.ts`, `meshWorker.ts`).
      ⚠️ Era o ponto perigoso: `Uint8Array.set(Uint16Array)` COMPILA e corta id ≥ 256 em
      silêncio — o typecheck passou verde antes de trocar. Teste: `viz` guarda 258.
- [x] `rules.ts` → `id <= MAX_BLOCK_ID`; `luz.ts` `chunkVazio` retipado.

### Tarefa 3 — Fio e disco com largura por chunk
- [x] `shared/src/chunkCodec.ts`: `bytesDoChunk`/`escreverChunk`/`lerChunk`/`lerChunkCru`.
      `lerChunk` recusa id > `MAX_BLOCK_ID` (mundo de versão mais nova → erro alto).
- [x] Snapshot `LJW1` (lê `LJW0`), lote `LJC1` (lê `LJC0`), save esparso `LJS3` (lê `LJS2`).
      `LJS1` continua sendo a magia do save denso — o snapshot dentro se identifica.
- [x] Cliente velho + servidor novo: magia desconhecida → o decode lança (erro alto).
- [x] Smokes `_smoke-pedir-coluna`/`_smoke-troca-raio` passaram a andar pelo byte de largura.
- [x] Tamanho: save denso de mundo sem id ≥ 256 cresce 1 byte por chunk (server/world.ljw:
      1 050 161 → 1 050 436 bytes).
- [x] **Regressão achada e corrigida antes do commit:** o `ehEstreito` célula a célula deixou o
      `encodeSave` 8× mais lento (35 ms vs 4,5 no mundo P) e o smoke do SIGINT passou a perder
      a corrida com o `npx`/`tsx` que embrulha o host (2 de 6 falhas; antigo 8 de 8 OK). Com a
      varredura em u32 (2 células por vez, máscara dos bytes altos): ~6 ms, 5 de 5 OK.

### Tarefa 4 — Provar com um bloco ≥ 256
- [x] Ida e volta de chunk largo com TODOS os 16 bits (inclusive 0xffff) no codec; World e
      vizinhança do mesher guardam 258. Snapshot/colunas/save com o maior id existente.
- [x] Cliente real: mundo denso (`npm run shots:loja`, verde) e mundo E por streaming (print).
- [ ] **Pendente:** o 1º bloco REAL com id ≥ 256 (colocar, salvar, streaming, mesher, luz,
      drop) — entra junto do primeiro bloco de circuito; hoje o maior id é 250.
- [ ] **Pendente:** `npm run bench:headless` antes/depois (memória e FPS no notebook da escola).

### Tarefa 5 — Fechamento
- [x] Comentário de `world.ts`, do `MAX_BLOCK_ID` (teto < 900), cerebrum e changelog.

## Armadilhas já sabidas

- **Id é byte de SAVE** — nada de renumerar os 0-250 existentes; bloco novo entra do 251 em
  diante, e os ≥ 256 só depois desta quest.
- O **atlas de texturas** tem seu próprio teto (256 tiles de 16×16, `mesher.ts:51`) — é outro
  limite, não resolvido aqui. Bloco novo que precise de tile próprio pode esbarrar nele antes.
- Sonda de verdade no fim (Chrome real, streaming, tablet), não só teste: o Do-Not-Repeat de
  2026-08-25 vale — mexer no caminho do chunk é mexer no que o aluno VÊ.
